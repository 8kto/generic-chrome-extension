import { getVariantsOptions } from 'components/popup/helpers'

describe('helpers test', () => {
  describe('getVariantsOptions', () => {
    it('adds 1 + 2 to equal 3', () => {
      expect(getVariantsOptions('')).toBe(3)
    })
  })
})
